# 3.0.6

- Fix NeoForge chunk load failures caused by a broken `ChainedBlockSource` constructor mixin inject.
- Restore Shadow-based list iteration for material rule sampling (avoids invalid array `<init>` remapping).
- Wire mixin refmap to the Loom-generated filename while keeping the Lithium mixin plugin.
