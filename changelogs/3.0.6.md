# 3.0.6

- Fix NeoForge chunk load failures caused by a broken `ChainedBlockSource` constructor mixin inject.
- Restore Shadow-based material rule sampling (`List` on ≤1.21.1, array on ≥1.21.2).
- Keep mixin plugin + refmap together so Forge/NeoForge can remap Yarn descriptors (fixes `NoiseChunkGeneratorMixin` / "No refMap loaded" crashes).
- Break Sinytra Connector on the Fabric jar — use the native Forge/NeoForge jar instead.
- Fix Modrinth/CurseForge publish tags: per-MC-range version numbers, full game-version expansion, and no stale jars from other branches.
