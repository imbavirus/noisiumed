# 4.0.0-beta.7 (1.21-1.21.1)

### NC-1 — NoiseChunk sample path

- **Monomorphic block-state samplers** — length-1/2/3 `ChainedBlockSource` chains replaced after `ChunkNoiseSampler` construction (`FastSingle/Dual/TripleBlockSampler`)
- **Interpolator array** — snapshot `interpolators` list once; `interpolateY/X/Z` use indexed loops (no `forEach`)
- PathMetrics: `nc1_s1` / `nc1_s2` / `nc1_s3` install counts

### Notes

Does not yet replace aquifer math or cell density grids (NC-2/NC-3).
