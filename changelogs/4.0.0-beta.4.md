# 4.0.0-beta.4 (1.21-1.21.1)

### Phase 2 — NoiseChunk interpolator path

- **DensityInterpolator** — inline Y/X/Z lerp (`a + t*(b-a)`) instead of `MathHelper.lerp` on every block step
- **PathMetrics** — `sample_pct` / `write_pct` (sampled every 8th L1 chunk) to attribute sampleBlockState vs direct writes

### Notes

Does not rewrite density graphs or aquifer fillers (later plan phases).
