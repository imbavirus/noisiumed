# 4.0.0-beta.9 (1.21-1.21.1)

### NC-3 — Cell density grid sample path

- `sampleBlockState` reads primary density from the last **CellCache** array (same data vanilla fills in `onSampledCellCorners`) then `AquiferSampler.apply`
- Skips redundant `density.sample()` virtual walk per block for the finalDensity+beardifier CacheAllInCell
- Ore veins still via secondary monomorphic sampler when present
- Disable: `-Dnoisiumed.cell.density.grid=false`
- PathMetrics: `nc3_grid=`

### Notes

Parity relies on CellCache layout matching `fillAllDirectly` index formula. Falls back to chain sample if cache size mismatches.
