# 4.0.0-beta.16.3 (1.21-1.21.1)

### Wall residual sample (failure-aware)

Learned from Phase 0/1: aquifer already slightly beats FN; quiet gap is populate residual.
No interps-deep, no heightmap/CellCache rewrites.

| Change | Why safe |
|--------|----------|
| **Ore Y band early-out** | Outside union of copper/iron `minY`/`maxY`, vanilla ore is always null — skip all vein DFs |
| **NC-3 solid path** | Cache `AquiferImplAccess`; precomputed cell strides; no per-block instanceof |
| **Solid default write** | `null` → default stone skips fluid post-process (solid path) |
| **DirectSectionWriter default** | Cache fluid/randomTick flags for default block |

Harness: multi mean/median/quiet (β16.2 tooling). Pin jar via `Resolve-NoisiumedJar.ps1`.
