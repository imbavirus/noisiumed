# 4.0.0-beta.16.4 (1.21-1.21.1)

### Structural wall: skip interpolator lerp under NC-3

**Failure lesson:** solid-path micros (16.3) could not close quiet forceload vs FN.
Spark showed populate still behind; interpolator Y/X/Z updates every block are wasted when
primary density comes from the cell cache.

| Change | Parity note |
|--------|-------------|
| `CellGridPositionAccess` | When NC-3 active, set `cellBlock*` + `sampleUniqueIndex` only |
| BulkNoiseFiller | Uses position-only instead of `interpolateY/X/Z` under cell grid |
| Aquifer / ore | Still see correct `blockX/Y/Z` via start+cell |

Does **not** skip `onSampledCellCorners` (still fills cell density cache).

Gate: quiet multi median vs FN + golden soft.
