# 4.0.0-alpha.1

### Added

- **L1 bulk noise path** when the generator is exact `NoiseChunkGenerator` and proto sections are empty/singular
  - Staging 4-bit section buffer + materialize
  - Deferred `OCEAN_FLOOR_WG` / `WORLD_SURFACE_WG` heightmaps
  - Aquifer fluid post-processing retained (no aquifer-only bailout)
- **Path metrics** (`PathMetrics`) for L0/L1 hit rate
- **ThreadLocal** `WorldgenScratchPool` for C2ME-friendly scratch
- `ChunkNoiseSamplerAccessor` for protected sampler APIs

### Changed

- `method_38332` shared via `NoisePopulateDispatcher` (Lithium + non-Lithium)
- Version line: 4.0.0-alpha.1 (1.21.6 primary)

### Notes

- Alpha: needs Spark + in-game parity validation before non-alpha release
- L2 fused surface and bulk biomes not included yet
