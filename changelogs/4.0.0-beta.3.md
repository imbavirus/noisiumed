# 4.0.0-beta.3 (1.21-1.21.1)

### Huge-win Phase 0 + Phase 1 (measurement + write path)

- **L1 direct section writes** — noise loop writes via palette `index` + `storage.set` (same resize-safe pattern as L0). Removes the staging **materialize** second pass.
- **PathMetrics** — L0 reason counters, L1 wall-time avg, direct write / sections-touched counts; `PathMetrics.snapshot()` / `Noisium.pathMetricsSnapshot()`.
- **PathSelector.Decision** — records why L0 was chosen (outside gen, retrogen, non-vanilla gen, unsafe sections, etc.).
- **Staging palette overflow** — hard fail instead of silent last-slot reuse (staging kept as legacy).
- Bench harness records wall-clock (boot, forceload, profile) and downloads Spark bins when possible.

### Notes

Still does **not** rewrite density evaluation or NoiseChunk interpolators (Phase 2–3 of the huge-wins plan).
