# 4.0.0-beta.6 (1.21-1.21.1)

### Phase 4 — Surface / column occupancy

- L1 records **column occupancy** (OR of section writers) and **has-solid** on `ChunkGenAttachment`
- `buildSurface` skips when L1 wrote no solids (no section scan)

### Phase 5 — Coverage

- L1 generator **allowlist** via `-Dnoisiumed.l1.generator.allowlist=fqcn1,fqcn2` or env `NOISIUMED_L1_GENERATOR_ALLOWLIST`
- Exact `NoiseChunkGenerator` still always allowed

### Notes

Column bits are available for future NoiseChunk / surface column work.
