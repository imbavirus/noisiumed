# 4.0.0-beta.1 (1.21–1.21.1)

### Added / completed 4.x stack

- **L0** — palette redirect with safe re-read after palette resize
- **L1** — adaptive staging palette (**4-bit → 8-bit** auto-upgrade, up to 256 states)
- **L1** — deferred WG heightmaps optional through surface (**L2**)
- **Biomes** — bulk palette pack + FixedBiomeSource chunk shortcut
- **L2-lite surface** — skip empty `buildSurface`; finalize deferred heightmaps on return/skip
- **ChunkGenAttachment** — per-chunk L1/heightmap-pending flags (identity map)
- **ChainedBlockSource** — cached sampler array + indexed loop
- Cell delta tables, last-state cache, column bit tracking on staging

### Fixed

- Materialize after singular palette growth (stale storage ref)

### Primary target

`1.21-1.21.1` / NeoForge 21.1.x
