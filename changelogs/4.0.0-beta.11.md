# 4.0.0-beta.11 (1.21-1.21.1)

### NC-5 — SpecDensity fill + monomorphic ore (A2/A3)

- **RangeChoice → SpecDensity** (`RangeSpecs.Choice`) via specializer
- **RegistryEntryHolder** resolves + specializes once (Ap2/Range under holders)
- **DensityInterpolator.fill** monomorphic `SpecDensity` path
- **Cell fill** monomorphic `SpecDensity.sample` call site
- **OreVeinSampler.create** → `FastOreVeinSampler` (no lambda; density graphs specialized)

PathMetrics: `nc5_ore=`

Note: full `sampleDensity` rewrite was attempted then reverted (broke density buffers / watchdog).
