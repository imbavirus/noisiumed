# GitHub Configuration
# Personal access token with repo scope
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# CurseForge Configuration (optional)
# Project ID from your CurseForge project page
CF_PROJECT_ID=your_curseforge_project_id

# CurseForge API token
# Get one at: https://console.curseforge.com/
CF_API_TOKEN=your_curseforge_api_token

# Comma-separated numeric version IDs (e.g., "9990,68441")
# If omitted, the script attempts to auto-resolve IDs
# CF_GAME_VERSION_IDS=9990,68441

# Comma-separated server/client environment IDs (e.g., "1,2")
# If omitted, the script attempts to auto-resolve Server (ID: 1) and Client (ID: 2)
# CF_ENVIRONMENT_IDS=1,2

# Java version ID (e.g., Java 21)
# If omitted, the script attempts to auto-resolve Java 21
# CF_JAVA_VERSION_ID=your_java_version_id

# Release type: release | beta | alpha (default: release)
# CF_RELEASE_TYPE=release

# Optional path to a changelog file (markdown/text)
# If not set, automatically looks for changelogs/{version}.md
# CF_CHANGELOG_FILE=changelogs/1.0.0.md

# Optional base URL for the upload API (default: https://minecraft.curseforge.com)
# CF_BASE_URL=https://minecraft.curseforge.com

# Modrinth Configuration (optional)
# Project ID from your Modrinth project page
MODRINTH_PROJECT_ID=your_modrinth_project_id

# Modrinth API token
# Get one at: https://modrinth.com/settings/api
MODRINTH_TOKEN=your_modrinth_api_token

# Comma-separated Minecraft versions (e.g., "1.21.1")
# If omitted, the script attempts to auto-detect from minecraft_version in gradle.properties
# MODRINTH_GAME_VERSIONS=1.21.1

# Comma-separated mod loaders (e.g., "neoforge", "forge", "fabric")
# If omitted, the script attempts to auto-detect "neoforge" based on the project
# MODRINTH_LOADERS=neoforge

# Release type: release | beta | alpha (default: release)
# MODRINTH_RELEASE_TYPE=release

# Optional path to a changelog file (markdown/text)
# If not set, automatically looks for changelogs/{version}.md
# MODRINTH_CHANGELOG_FILE=changelogs/1.0.0.md
